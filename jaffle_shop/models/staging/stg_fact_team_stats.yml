models:
  - name: stg_fact_team_stats
    columns:
      - name: match_id
        tests:
          - not_null
          
    tests:
      - dbt_utils.unique_combination_of_columns:
         arguments:
          combination_of_columns:
            - match_id
            - team_id                                                                                                    
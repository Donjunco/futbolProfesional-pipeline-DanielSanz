version: 2

# Try using these source definitions instead!
sources:
  - name: raw_source
    schema: RAW
    description: DATOS SIN LIMPIAR DE LA BASE DE proyecto2_dbt
    database: PROYECTO2_DBT
    tables:
      - name: DIM_COMPETITION
      - name: DIM_COUNTRY
      - name: DIM_MANAGER
      - name: DIM_REFEREE
      - name: DIM_SEASON
      - name: DIM_STADIUM
      - name: DIM_TEAM
      - name: FACT_MATCHES
      - name: FACT_TEAM_STATS